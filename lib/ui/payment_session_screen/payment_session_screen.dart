import 'package:flutter/material.dart';

class PaymentSessionScreen extends StatelessWidget {
  const PaymentSessionScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: Center(
        child: Container(
          color: Colors.deepOrangeAccent,
        ),
      ),
    );
  }
}
